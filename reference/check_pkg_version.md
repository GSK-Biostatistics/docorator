# Check package versions for docorator object are the same as loaded

Check package versions for docorator object are the same as loaded

## Usage

``` r
check_pkg_version(x)
```

## Arguments

- x:

  docorator object
