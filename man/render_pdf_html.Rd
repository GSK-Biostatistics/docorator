% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/render.R
\name{render_pdf_html}
\alias{render_pdf_html}
\title{Render to PDF via HTML}
\usage{
render_pdf_html(
  x,
  display_loc = NULL,
  header = NULL,
  footer = NULL,
  keep_html = TRUE,
  wait = 3
)
}
\arguments{
\item{x}{\code{docorator} object}

\item{display_loc}{optional path to save the output pdf to}

\item{keep_html}{Whether to keep the intermediate HTML file. If \code{TRUE}
(default), the HTML is saved alongside the PDF with the same base name.
If \code{FALSE}, HTML is deleted after conversion.}

\item{wait}{Number of seconds to wait after page navigation before printing.
Increase if the table takes time to render. Defaults to \code{3}.}
}
\value{
Invisibly returns the path to the created PDF file.
}
\description{
Render to PDF via HTML
}
\section{Examples}{


\if{html}{\out{<div class="sourceCode r">}}\preformatted{gt::gtcars |>
  dplyr::slice_head(n = 10) |>
  dplyr::select(mfr, model, year, msrp) |>
  gt::gt() |>
  as_docorator(
     display_name = "output",
     header = fancyhead(
       fancyrow(left = "Study ABC-123", right = "Draft"),
       fancyrow(center = "Table 1: Vehicle Summary")
     ),
     footer = fancyfoot(
       fancyrow(left = "Source: gtcars"),
       fancyrow(left = "program.R", right = format(Sys.Date(), "\%d\%b\%Y"))
     )
  ) |> 
  render_pdf_html()
}\if{html}{\out{</div>}}
}

